machine:
  ruby:
    version: 2.3.4
dependencies:
  pre:
    - gem install bundler --pre

deployment:
  production:
    branch: master
    commands:
      - heroku run rake db:migrate
    heroku:
      appname: training-ci
