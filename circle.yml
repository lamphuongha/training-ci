machine:
  ruby:
    version: 2.3.4
dependencies:
  pre:
    - gem install bundler --pre
    - heroku run rake db:migrate --app training-ci

deployment:
  production:
    branch: master
    heroku:
      appname: training-ci
