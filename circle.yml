dependencies:
  pre:
    - gem install bundler --pre

deployment:
  production:
    branch: master
    heroku:
      appname: training-ci
    commands:
      - git push git@heroku.com:training-ci.git $CIRCLE_SHA1:master
      - heroku run rake db:migrate --app training-ci
